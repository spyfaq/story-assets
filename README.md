# story-assets

Public staging area for daily Instagram Story images.

Instagram's Content Publishing API does not accept image bytes - it fetches a
public URL - so each day's graphic is committed here as JPEG just long enough
for Meta to download it. Nothing else lives here.